# @joncarlson/prettier-config

## Usage

**Install**:

```bash
npm install --save-dev --save-exact prettier
npm install --save-dev @joncarlson/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@joncarlson/prettier-config"
}
```